# Supported V1 Engine Scope

Supported in v1:
- deterministic parsing and IR lowering for representative batch-like and request-like programs
- bounded human-guided coding-agent implementation passes on slices
- runtime-backed Java generation
- deterministic validation and feedback bundles
- whole-program evidence capture for one logical COBOL program mapped to one Spring Boot microservice
